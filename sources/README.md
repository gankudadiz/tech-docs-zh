# sources

这里用于保存官方文档原文、HTML 快照、清洗前资源和采集清单。

建议结构：

```text
sources/{product}/{version}/raw/
sources/{product}/{version}/normalized/
sources/{product}/{version}/raw-assets/
sources/{product}/{version}/manifest.yml
```

不要把没有来源、许可和采集日期的信息放入正式翻译内容。

